#include "main.h"
/**
 * set_string - function to set a string
 *
 * @s: double pointer
 * @to: pointer variable
 */
void set_string(char **s, char *to)
{
	int i;

	while (**s)
	{
		for (i = 0 ; i != '\0' ; i++)
		{
			to = s;
		}
	}
}
