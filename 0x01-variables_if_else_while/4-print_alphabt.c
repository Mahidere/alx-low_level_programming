#include <stdio.h>
/**
 * main - Entry point
 *
 * for loop
 *
 * if condition
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char lower;

	for (lower = 'a' ; lower <= 'z' ; lower++)
	{
		if (lower != e && lower != q)
		{
			putchar(lower);
		}
	}
	return (0);
}
