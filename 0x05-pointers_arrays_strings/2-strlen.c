#include "main.h"
/**
 * _strlen - Returns the length of a string
 *
 * @s: variable
 *
 * Return: Always 0 sucees
 */
int _strlen(char *s)
{
	int l;

	for (l = 0 ; *s != '\0' ; l++)
	{
		s++
	}
	return (l);
}
